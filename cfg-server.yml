app:
  message: "SEARCH365"
  title: "Swagger"
  version: "5.0.0"
  features:
    enableForecast: true
    enableExtendedForecast: false
    maxDays: 10001
    enableAlerts: true
  limits:
    maxRequestsPerMinute: 1000
    maxConcurrentUsers: 500
  endpoints:
    externalApi: "https://api.weather.com/v3"
    alertApi: "https://api.alerts.com/v1"
